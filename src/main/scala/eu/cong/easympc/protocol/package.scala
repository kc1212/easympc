package eu.cong.easympc

/**
  * Protocol implements a few MPC protocols.
  * The simplest is the Beaver protocol, implemented in BeaverActor.
  * The design assumes a trusted dealer for the Beaver triples and
  * a trusted bulletin board for posting the (masked) multiplication
  * shares.
  */
package object protocol {}
